<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.meugen.android.levelup.homework1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".Homework">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ActivityA"
            android:label="ActivityA" />
        <activity android:name=".activities.ActivityB"
            android:label="ActivityB" />
        <activity android:name=".activities.ActivityC"
            android:label="ActivityC" />
        <activity android:name=".activities.ActivityD"
            android:label="ActivityD" />
    </application>

</manifest>